"use client";
import Link from "next/link";import { Formik,Form,Field,ErrorMessage } from "formik";import { toast } from "sonner";import { signinSchema } from "@/lib/validators";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";
export default function SignInPage(){return <div className="container max-w-md py-16"><h1 className="mb-6 text-3xl font-bold">Sign In</h1><Formik initialValues={{email:'',password:''}} validationSchema={signinSchema} onSubmit={()=>toast.success('Signed in successfully')}><Form className="space-y-4"><div><Field as={Input} name="email" placeholder="Email"/><p className="text-sm text-red-500"><ErrorMessage name="email"/></p></div><div><Field as={Input} name="password" type="password" placeholder="Password"/><p className="text-sm text-red-500"><ErrorMessage name="password"/></p></div><Button className="w-full" type="submit">Sign In</Button></Form></Formik><p className="mt-4 text-sm text-muted-foreground">No account? <Link className="text-primary" href="/auth/signup">Create one</Link></p></div>}
